import type { Config } from 'tailwindcss';

const config: Config = {
  content: ['./src/**/*.{html,ts}', './src/styles.css'],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
